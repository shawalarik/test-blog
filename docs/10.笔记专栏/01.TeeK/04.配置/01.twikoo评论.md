---
title: twikoo评论
date: 2025-07-14 07:01:02
permalink: /twikoo
categories:
  - twikoo
tags:
  - twikoo
coverImg: /home/bg05.webp
---

## 版权

::: tip 提示
本文是在 博主[One](https://onedayxyy.cn/) 文章：[《配置 twikoo 评论系统》](https://onedayxyy.cn/teeker-twikoo) 和 [Hyde](https://teek.seasir.top/) 的基础上增加了自己实践过程的一些细节，转载无需和我联系，但请注明文章来源。如果侵权之处，请联系博主进行删除，谢谢~
:::

## Twikoo 的安装

官网：https://twikoo.js.org

Twikoo 支持多种部署方式：云函数，Vercel，私有部署、Docker.... 具体参见文档：https://twikoo.js.org/backend.html

我这里选择docker部署，因为这是最方便的方式

### docker 部署 😜

```sh
docker run --name twikoo -e TWIKOO_THROTTLE=3000 -p 8080:8080 -v ${PWD}/data:/app/data -d imaegoo/twikoo
```

TWIKOO_THROTTLE为评论间隔时间，单位是毫秒

```bash
-v ${PWD}/data:/app/data
```

这里挂载卷时 `${PWD}/data`最好指定一个合适的目录，方便后期的数据备份、日志分析等。

```sh{2,4}
[root@dl twikoo]# pwd
/dl/docker/twikoo
[root@dl twikoo]# ls
db.json  db.json.0  db.json.1  db.json.2
[root@dl twikoo]# 
```

## 配置 HTTPS

理论上这样部署，就完成后台的部分了，**但鉴于我的网站用了 HTTPS，而 Twikoo 本身并不支持，因此还需要做反向代理**。

## 配置 Nginx 反向代理

我的配置如下，省略了部分无关的配置项，之前使用docker将Twikoo 运行在8080端口，所以这里将 /twikoo 开头的请求转发到 twikoo 服务

```nginx
server {
    listen 80 ; 
    listen 443 ssl http2 ; 
    server_name dl-web.top; 
    index index.php index.html index.htm default.php default.htm default.html; 
    proxy_set_header Host $host; 
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; 
    proxy_set_header X-Forwarded-Host $server_name; 
    proxy_set_header X-Real-IP $remote_addr; 

    # 将 /twikoo 开头的请求转发到 twikoo 服务
    location /twikoo {
        proxy_pass http://localhost:8080; 
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

配置完成后需要重启 NGINX 服务

```sh
nginx -s reload
```

## 前端配置 Twikoo

编辑 `配置文件`文件，修改为如下代码：

```js
// 评论配置
export const Comment = {
  provider: "twikoo",
  options: {
    // twikoo 配置，官网：https://twikoo.js.org/
    envId: "https://自己的ip或者域名/twikoo",
    // link: "https://cdn.jsdelivr.net/npm/twikoo@1.6.41/dist/twikoo.all.min.js", //teek主题内置
    version: "1.6.41",

    // waline 配置，官网：https://waline.js.org/
    // serverURL: "https://tk.waline.youngkbt.cn/",
    // jsLink: "https://unpkg.com/@waline/client@v3/dist/waline.js",
    // cssLink: "https://unpkg.com/@waline/client@v3/dist/waline.css",

    // giscus 配置，官网：https://giscus.app/zh-CN
    // repo: "Kele-Bingtang/vitepress-theme-teek",
    // repoId: "R_kgDONpVfBA",
    // category: "Announcements",
    // categoryId: "DIC_kwDONpVfBM4Cm3v9",

    // artalk 配置，官网：https://artalk.js.org/
    // server: "",
    // site: "",
  }
};

```

## 验证

打开博客页面下方进行验证即可
